{
  "name": "cypress_project",
  "version": "1.0.0",
  "devDependencies": {
    "cypress": "^14.5.3",
    "cypress-mochawesome-reporter": "^4.0.2"
  },
  "dependencies": {
    "@faker-js/faker": "^9.9.0",
    "npm": "^11.5.2"
  },

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run": "cypress run",
    "report:clean": "rm -rf cypress/reports",
    "cy:report": "npm run report:clean && npm run cy:run"
  }

}
